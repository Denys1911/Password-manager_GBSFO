import React from "react";

import './Spinner.css';

const Spinner = () => (
    <div className="d-flex mx-auto spinner">
        <div className="spinner-inner">
            <div/>
            <div/>
        </div>
    </div>
);

export default Spinner;